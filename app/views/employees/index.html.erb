<h1>Employees</h1>
<% @employees.each do |employee| %>
  <%= link_to(employee.first_name + ' ' + employee.last_name, employee_path(employee)) %><br>
<% end %>

<%= button_to('Add Employee', new_employee_path) %>